---
import Base from '../layouts/Base.astro';
import TokensPage from '../components/TokensPage.vue';
---

<Base title="Tokens">
  <div class="page-header">
    <h1 class="page-title">Bearer tokens</h1>
    <p class="page-subtitle">
      Interactive sign-in doesn't need this page — oauth2-proxy handles browser sessions
      transparently. This page is for bootstrapping <strong>programmatic</strong> MCP clients (like Claude
      Desktop) that don't yet support OAuth discovery: mint a static Bearer token here, paste it into
      the client's config, and revoke it whenever you're done with it.
    </p>
    <p class="page-subtitle page-subtitle--scope">
      These tokens only grant access to <strong>this broker</strong> (<code
        >mcp.af.uchicago.edu</code
      >) — they are not the credentials backend services like Rucio or your CERN account use. For
      those, see the <a href="/identities/">Identities page</a>.
    </p>
  </div>

  <TokensPage client:load />
</Base>

<style>
  .page-header {
    margin-bottom: 1.75rem;
  }

  .page-title {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 1.5rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    color: var(--color-af-text);
    margin: 0 0 0.5rem;
  }

  .page-subtitle {
    font-size: 0.9375rem;
    color: var(--color-af-dim);
    line-height: 1.6;
    margin: 0;
    max-width: 52rem;
  }

  .page-subtitle--scope {
    margin-top: 0.5rem;
  }

  .page-subtitle a {
    color: var(--color-af-teal);
  }

  .page-subtitle code {
    font-family: 'IBM Plex Mono', monospace;
    font-size: 0.8125rem;
  }
</style>
